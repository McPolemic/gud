# Rough idea for each git command
* Talk about a git command
* Use it and observe the effects
* Roughly implement it
* Pull out helper commands (in order to recreate plumbing like `git cat-file`, `git write-tree`)
